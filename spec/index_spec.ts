declare var describe, expect, it;

// require('es6-shim');
// require('reflect-metadata');

describe('ngrx-local-storage', () => {
    it('should just work', () => {
        expect(true).toBe(true);
    });
});